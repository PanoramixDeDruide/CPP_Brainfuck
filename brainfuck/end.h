#include <memlookup.h>
#if MODE == 1
Program terminated with memory address MEM_ADDRESS and value XMEMLOOKUP(MEM_ADDRESS) at depth DEPTH
#else
Program terminated with memory address MEM_ADDRESS_ and value XMEMLOOKUP(MEM_ADDRESS_) at depth DEPTH_
#endif
